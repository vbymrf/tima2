-- 0018: убрать открытые персональные данные. Завершение 0017.
--
-- 0017 добавляла шифртекст и слепой индекс рядом с открытыми колонками, а перенос
-- существующих строк делался кодом (backfill) — миграции применяются раньше него,
-- поэтому дропнуть колонки в том же релизе было нельзя.
--
-- Переносить оказалось нечего: база пересоздаётся с нуля, данных для сохранения нет
-- (ПЛАН-РЕФАКТОРИНГА.md, решение 13). Backfill удалён вместе с этой миграцией.
--
-- ПОЧЕМУ phone_bidx/phone_enc ОСТАЮТСЯ NULLABLE, хотя база чистая и соблазн есть:
-- временные аккаунты заводятся БЕЗ телефона (key-lifecycle.md §7). NOT NULL пришлось
-- бы снимать на следующем же этапе. Уникальность держит частичный индекс из 0017.

ALTER TABLE users
    DROP COLUMN IF EXISTS phone,
    DROP COLUMN IF EXISTS display_name;

ALTER TABLE sms_codes
    DROP COLUMN IF EXISTS phone;

-- Индекс по открытому номеру уходит вместе с колонкой; слепой индекс из 0017 остаётся.
DROP INDEX IF EXISTS idx_sms_codes_phone;
