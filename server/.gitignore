# Сгенерированный protobuf-код (scripts/gen-proto.sh, ADR-0009) КОММИТИМ:
# сервер должен собираться из чистого клона без proto-тулчейна (buf/protoc-gen-go).
# Регенерация перезапишет эти файлы — тогда просто закоммить изменения.
