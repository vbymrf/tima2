# Окно «Телефон» — заглушки

| Что стоит сейчас | Что должно быть | Чем держится |
|---|---|---|
| «шестерёнка» открывает «Устройства» | подокно «Настройки» (`интерфейс.md §396`) | подокно не построено |
| «Группа» в шапке — **вход придуман, источника нет** | группы живут в каталоге окна 2 (`§51`, `§702`) | окна 2 нет; временный вход, чтобы проверять группы живьём |
| Вкладок нет | Контакты · Чаты · Звонки | «Контакты»: данные готовы, экрана нет; «Звонки» ждут К7 |
| Присутствие не показывается | «в сети», «была 5 минут назад» | сервер не отдаёт (`internal/api/ws.go`) |
| Кнопки шапки — стрелки | значки поиска и настроек | набора значков нет |
