# Отчёт: К0 расчистка, К1 каркас

> Ведётся по ходу работы 2026-08-20. Дописывается сверху вниз, записи не правятся.
> План — [Plan.md](../Plan.md), порядок — [ДОРОЖНАЯ-КАРТА.md](../ДОРОЖНАЯ-КАРТА.md).
>
> **Читать можно с любого места:** каждый срез заканчивается коммитом и хешем.
> Комментарии, требующие вашего внимания, помечены **⚠ РЕШЕНИЕ** и собраны в конце.

---

## К0.1 — инвентарь поведения · сделано

**Результат:** [инвентарь-поведения.md](../инвентарь-поведения.md), **16 пунктов**
вместо запланированных 10.

Шесть нашлись при вычитке и в плане их не было. Три из них стоит прочитать:

**⚠ РЕШЕНИЕ · пункт 13 — сборка только под `arm64-v8a`.** В `build.gradle.kts` v1
оставлен один ABI (нативные WebRTC и zstd под все ABI раздувают APK), и там же
записано капсом: **эмулятор x86_64 перестаёт работать, проверять надо на телефоне.**
Это упирается прямо в К8: ферма либо из живых телефонов, либо в v2 собирается второй
ABI для отладочных сборок. Заложить дешевле в К1, поэтому пункт поднят туда.

**Пункт 14 — подпись APK.** Отладочный ключ генерируется на каждой машине свой,
поэтому смена машины сборки давала «Приложение конфликтует с другим пакетом» у всех
установленных. Ключ в `keystore/` вне гита, его потеря = переустановка у всех. Плюс
распространяется **debug**-сборка со стабильной подписью: `release` потребовал бы
отдельной проверки R8.

**Пункт 12 — уведомление о загрузке обновления даёт только прогресс, без «нажми,
чтобы открыть».** По такому нажатию APK открывает система своим обработчиком, и на
Samsung им оказывается посторонний компонент — разрешение на установку она просит для
него, а не для нас.

Заодно уточнилось то, что в плане было сказано приблизительно:

- потолок zstd-бомбы — **16 МиБ, и проверяется до распаковки** через
  `Zstd.getFrameContentSize`; проверка после распаковки — уже съеденная память;
- `LinkState` — не «три состояния», а таблица из **13 признаков** в тексте
  исключения, каждый из журналов настоящих испытаний;
- `EscrowTrust.ENCLAVE_SIGNING_PUBLIC_KEY = null`, и проверка подписи анклава при
  этом **тихо пропускается**. В v2 переезжает как есть, но пропуск надо сделать
  видимым в диагностике.

Все 16 пунктов разложены по этапам: 13–15 в К1 (сборка и распространение), 1–3 в
К3.1, 4–6 в К2, 7–10 в К3, 11–12 в К7, 16 в К5.

---

## К0.2 — наблюдение v1 в работе · не выполнено, причина внешняя

Собранные `TIMA-*.apk` в корне на месте (последний — 0.6.2). **Поставить и посмотреть
не могу:** на этой машине нет ни Android SDK, ни adb, ни подключённого телефона.

Это не блокирует К0.3 и К0.4: возврат к v1 обеспечивает тег, а не запущенное
приложение. Но **наблюдение v1 остаётся неоплаченным долгом**: часть поведения
(поведение шапки при плохой связи, вид очереди в интерфейсе, тайминги переходов)
инвентарём не покрывается — оно видно только глазами. Если такая сверка нужна, её надо
провести до того, как v1 забудется, и на машине с телефоном.

## К0.3 — тег `ref/client-v1` · сделано

Тег создан и **выложен на удалённый** (это условие: тег, живущий на одной машине, не
защищает ни от чего). Возврат к исходникам: `git checkout ref/client-v1 -- client/`.

## К0.4 — удаление `client/` · сделано

Удалено 97 файлов из индекса. Два следствия, которых в плане не было:

**1. Все 15 пусковых скриптов из корня стали мёртвыми** — каждый прямо или через
`.bat`-обёртку зовёт сборку из `client/`. Перенесены в
`doc_mig/arhiv/скрипты-v1/` с описанием: не для запуска, а как последовательность
действий при написании пусковых скриптов v2 (К1). Там же записано правило репозитория
про BOM у `.ps1` с кириллицей — оно понадобится сразу.

**2. Два рабочих скилла агентов ссылались на удалённое дерево** и были бы источником
путаницы у следующего агента: он пошёл бы собирать `:composeApp`. Помечены шапкой
«устарело в части клиента»:

- `.cursor/skills/adaptive-test-runner/project/PROFILE.md` — тиры клиента не работают;
  `go-unit` и `messenger-crypto` действуют, их ничто не трогало;
- `.cursor/skills/adaptive-ops-runner/project/rules.md` — правила запуска приложения
  и десктопа не действуют.

Регистр тиров переписывается в К1.8, вместе с появлением CI.

**Не тронуто намеренно:** `doc/adr/0011-message-content-model.md` тоже ссылается на
файл в `client/`. Это ADR — журнал решений, и правка в нём допускается только в
ссылках; висячая ссылка в историческом документе честнее переписанной истории.
Поправится в дорожке Д одним проходом со всеми ссылками.
