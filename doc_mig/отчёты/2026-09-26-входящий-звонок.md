# Входящий звонок — ход работ

План: [ПЛАН-ВХОДЯЩЕГО-ЗВОНКА.md](../ПЛАН-ВХОДЯЩЕГО-ЗВОНКА.md). Задание заказчика
2026-09-26: «Выполни план целиком или до развилки. Коммить срезы и заполняй отчёты».

Ветка `stand`, замка нет. Коммиты локальные, пуш — по просьбе.

## Срезы

| Срез | Что | Коммит | Проверено |
|---|---|---|---|
| ВЗ0а, сервер | лента `cts`: 0056 (`call_tops`, `call_updates`, `call_cursors`, `calls.delivered_at`), изменения вызова/ответа/конца, `call.ack` в канале, доставка и «не в сети» по подтверждению, серверный срок 50 с → `missed`, `GET /calls/updates`, `POST /calls/seen`, уборщик — сутки; вебхук: `answered` у личного — только от собеседника | `c1958756` | `go build`, `go vet`, архитектурная проверка (бюджет Store 212 → 218). **Тесты с базой не прогонялись** — Postgres на машине нет; тесты ленты написаны (`calls_unreachable_test.go`), пройдут на стенде |
| ВЗ0а, клиент | протокол (`cts` в приветствии, `call.poke {cts}`, `call.ack`), `Calls.updates/seen`, `CallLedger` (правила по пачке), `Receiver.callsTop` (свой номер, отметка пропущенных, разрыв → журнал), «Вызов…» → «Звонит…», исходы словами, пропущенный уведомлением, «seen» при открытии «Звонков» | `99c7ba08` | тесты `shared` (9 новых `CallLedgerTest`), `core-network`, `core-call`, `feature-chat`, `feature-call`, `core-words`; сборка Android. `WordsBudgetTest` красный как и до работы (90 / 70) |

## Развилки и решения по ходу

- **Групповые звонки оставлены на прежней дороге** (журнал устройства, `call.incoming` /
  `call.state`, `call.poke {call_id}`). В план они не входят, а их переделка удвоила бы срез.
- **Найдено по ходу: вебхук LiveKit объявлял личный звонок отвеченным, когда в комнату
  входил сам звонящий** — а он входит сразу. С лентой это дало бы звонящему ложное
  «ответили»; починено тем же срезом.
- **`taken` как отдельное слово исчезло**: «ответили на другом устройстве» — это `answered`
  с `here = false` в ответе ленты, который составлен для спрашивающего устройства.

## Что не проверено и почему
