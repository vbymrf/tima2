# План: журнал звонков — Ж

Заведён 2026-09-23 по решению заказчика. Вкладка «Звонки» в окне «Телефон» — заглушка с
самого начала; здесь она наполняется.

Родственное: [ADR-0025](../doc/adr/0025-call-events.md) — события звонка,
[ПЛАН-ДОРАБОТКИ-ЗВОНКОВ.md](ПЛАН-ДОРАБОТКИ-ЗВОНКОВ.md) — окно 0 и кнопки,
[ПЛАН-ДОСТАВКИ-ПОЛОСАМИ.md](ПЛАН-ДОСТАВКИ-ПОЛОСАМИ.md) — ручка `GET /calls/{id}`, на
которой журнал стоит одной ногой.

---

## 0. Половина журнала уже есть, и это надо знать до начала

Таблица `calls` на сервере пишется с первого дня звонков и держит **всё, что нужно строке
журнала**:

| Поле | Что даёт строке |
|---|---|
| `initiator_id`, `peer_id` | кто кому — и отсюда же **направление** |
| `kind` | **аудио или видео** — та самая кнопка, которой звонили |
| `state` | `ringing` · `answered` · `ended` · `missed` · `busy` · **`lost`** — оборвался, конец неизвестен |
| `created_at`, `answered_at`, `ended_at` | когда начался, взяли ли трубку, сколько длился |
| `type`, `group_id` | звонок один на один или групповой |

**Ничего записывать не нужно — нужно отдать и показать.** Это меняет цену работы: не
«завести журнал», а «дотянуть две ручки до экрана».

**Расхождение в ADR-0025, которое надо поправить вместе с Ж2.** Там записано: «Журнала
звонков как хранимой вещи по-прежнему нет: ни одно из перечисленных событий никуда не
ложится». Про кадры и ленту это верно, про состояние в базе — нет: оно ложится и живёт.
Строка вводит в заблуждение и правится вместе с появлением экрана.

## 1. Одна строка — два разных рассказа

Главное решение экрана, и оно не в вёрстке: **строка журнала у двоих читается по-разному**,
потому что запись в базе одна на обоих.

| В базе | У звонившего | У того, кому звонили |
|---|---|---|
| `answered` | исходящий, 3 мин | входящий, 3 мин |
| `missed` | **не дозвонился** | **пропущенный** |
| `busy` | собеседник занят | ничего: у него в это время шёл разговор |
| `ended`, `answered_at` пуст | отменил | **отклонил** |

Первые три выводятся из того, что есть. Четвёртая — **не выводится**, и это единственная
дыра в данных: `POST /end` зовут обе стороны, и кто нажал первым, в базе не остаётся. Без
этого «отменил» и «отклонил» неразличимы, а человек их различает.

Лечится одним полем `ended_by` — см. Ж4. Поле аддитивное, миграция безопасная.

## 2. Что показываем в строке

```
🔴  Аня Борисова                     вчера 14:32   📹
    ↗ видео · не дозвонился
```

**Кнопка в строке одна — та, которой звонили** (решение заказчика 2026-09-23). Видео
звонили — видео и перезвонит; голосом — голосом. Двух кнопок в строке журнала нет и не
нужно: журнал отвечает на «что было», и повтор продолжает то же самое, а не предлагает
выбор заново.

| Часть | Откуда |
|---|---|
| Имя и лицо | справочник поверх книги — тем же механизмом, что везде (Д14) |
| Стрелка | `initiator_id == я` → исходящий |
| **Вид** | `kind`: «видео» или «аудио» — **решение заказчика 2026-09-23** |
| Исход | из таблицы §1 |
| Время | `created_at`; «сегодня», «вчера», дата |
| Длительность | `ended_at − answered_at`, только если трубку взяли |

**Пропущенные выделяются.** Иначе журнал — ровный список, в котором главное (кому я не
ответил) ищут глазами.

## 3. Две кнопки в «Контактах»

**Решение заказчика 2026-09-23, обратное прежнему.** 2026-09-20 было сказано: «В Контакты,
Звонки — делаем кнопку позвонить. Кнопку видео звонок туда не кладём». Теперь кладём, и
прежнее решение отменено, а не забыто.

Сегодня в строке книги справа одна кнопка: «позвонить» тому, у кого есть аккаунт, или
«пригласить» тому, у кого нет. Становится две.

**Чем это стоит платить, и это надо решить до вёрстки.** Строка книги на Redmi — 360 точек
в ширину на имя, телефон и кнопки. Две круглые кнопки по 36 точек с зазором съедают
82 точки, то есть почти четверть строки; длинные имена начнут обрезаться раньше.

Три способа были, развилка Ж-В1 — **выбрано (а), 2026-09-23**:

| | Как | Чем платим |
|---|---|---|
| **а** ✔ | две круглые кнопки рядом | четверть ширины строки; имена обрезаются раньше |
| **б** | одна кнопка, долгое нажатие — видео | ширина цела, но **долгое нажатие не видно**: о нём узнают, только если рассказали |
| **в** | кнопка открывает карточку человека, там уже есть четыре действия | ноль новой вёрстки, но звонок становится в два нажатия |

Сделано: голос слева, видео справа, обе только у тех, у кого есть аккаунт. Порядок не
случаен — голосом звонят чаще, а правый край строки ближе к большому пальцу.

## 4. Откуда журнал берётся

**С сервера, а не с устройства.** Три довода, и первый решающий:

1. **Устройств у человека несколько.** Журнал, собранный на телефоне, на планшете будет
   другим — а это одна и та же жизнь. У сервера он один.
2. **Данные уже там** и пишутся независимо от того, было ли приложение живо: звонок,
   пропущенный при выключенном телефоне, в журнале будет.
3. **Клиенту нечего хранить**: строк мало, экран открывают редко, кэш ради этого заводить
   рано.

Ручка — `GET /api/v1/calls?limit=&before=`, страницами по времени. Отдаёт только те
звонки, где спрашивающий — сторона или участник группового.

~~**Глубина журнала сегодня — 90 дней**, и она **не своя**~~ — **развязано 2026-09-23
(Ж5, 0054).** Строки убирались по `delivery_retention_days`, тому же сроку, что и доставка
событий; сцепка была тихая — опусти срок доставки ради места, и журнал звонков молча стал
бы короче. Теперь свой ключ `calls_journal_days = 180`.

## 5. Задачи

| | Что | Чем упирается | Состояние |
|---|---|---|---|
| **Ж1** | `GET /api/v1/calls` — список звонков стороне, страницами | ничем | **сделано 2026-09-23** |
| **Ж2** | Вкладка «Звонки»: строки с видом, исходом, временем и длительностью; пропущенные выделены | Ж1 | **сделано 2026-09-23** |
| **Ж3** | Кнопка «Видео звонок» в «Контактах» | Ж-В1 | **сделано 2026-09-23** |
| **Ж4** | `ended_by` — отличить «отменил» от «отклонил» | ничем | **сделано 2026-09-23** |
| **Ж5** | Свой срок хранения журнала, отдельно от срока доставки | ничем | **сделано 2026-09-23** |
| **Ж6** | Позвонить из журнала — **тем же видом**, каким звонили тогда | Ж2 | **сделано 2026-09-23** |
| **Ж7** | Счётчик пропущенных на вкладке | Ж-В3 | **сделано 2026-09-23** |
| **Ж8** | Проверки: направление и исход у обеих сторон, вид сохраняется, страницы | Ж1, Ж2 | клиентские зелёные, **серверные не прогонялись** — нет базы |

### Что сделано 2026-09-23 и чем это проверено

Подробный разбор — [отчёты/2026-09-23-журнал-звонков-Ж.md](отчёты/2026-09-23-журнал-звонков-Ж.md).

| Где | Что легло |
|---|---|
| `server/migrations/0054_calls_journal.sql` | `calls.ended_by`, политика `calls_journal_days = 0` («не удалять»), индекс по `initiator_id` |
| `server/internal/store/calls.go` | `CallRow`, `ListCalls`, `SetCallState` четвёртым доводом `endedBy` |
| `server/internal/api/calls_journal.go` | `GET /api/v1/calls`, сборка строки на проводе (`callJSON`) |
| `server/internal/api/group_calls.go` | вход в звонок перечисляет **живые** состояния, а не хоронит по списку |
| `server/internal/worker/worker.go` | `ringing` → `missed`, `answered` → `lost`; уборка при нуле пропускается |
| `client/.../CallLog.sq`, `12.sqm`, `SqlCallLog.kt` | журнал в местной базе — копия, с одним местным полем `seen` |
| `client/.../CallLog.kt`, `CallHistory.kt` | `CallRecord.outcome` — одна запись, два рассказа |
| `client/.../CallHistoryOverHttp.kt` | страница журнала с сервера; `null` — «не дошли», а не «пусто» |
| `client/.../CallsScreen.kt`, `CallsStore.kt` | вкладка «Звонки», счётчик, повтор тем же видом |
| `client/.../Storage.kt`, `Root.kt` | «Память и трафик»: срок и число строк |
| `client/.../BookScreen.kt` | две кнопки в строке книги: голос и видео |

**Проверено:** весь `jvmTest` клиента — 1052 теста, ноль падений и ноль пропусков; из них
13 новых (7 `CallRecordTest`, 6 `CallsStoreTest`). `go build`, `go vet`,
`TestArchitectureBudgets` — чисто.

**Не проверено, и это надо знать.** Семь серверных проверок в `calls_journal_test.go`
написаны, но **ни одна не прогонялась**: на машине нет ни Docker, ни Postgres, и все
тесты `internal/api` уходят в `t.Skipf`, а `go test ./...` печатает при этом `ok`. Первый
живой прогон журнала — на стенде.

**Две красные проверки клиента** — `CurrentWordsTest` 5/4 и `WordsBudgetTest` 90/70 —
достались от прежних работ и этим заходом не тронуты: числа не сдвинулись ни на единицу.

### Бюджет методов `*Store`: 206 → 210

Проверка `TestArchitectureBudgets` стояла **красной** ещё до этой работы: разбор звонков
2026-09-20 привёз `HasLiveCall`, `OpenCalls` и `GCCalls`, а бюджет не тронул. Записаны
задним числом, вместе с `ListCalls` этой работы. Это ровно тот случай, ради которого
бюджет заведён, — рост, который никто не назвал.

«Кто положил трубку» при этом методом не стало: оно пишется четвёртым доводом того же
`SetCallState`. Отдельный метод рядом с существующим значил бы два способа менять одну
строку.

### Ж1 — что отдаёт ручка

Строка ответа — **та же, что у `GET /calls/{id}`** из плана доставки, только списком. Это
не экономия, а требование: два разных вида одного и того же однажды разойдутся, и
разойдутся молча.

### Ж6 — почему «тем же видом»

Повторный звонок из журнала — самое частое действие в любом телефоне. Человек, не
дозвонившийся видео, жмёт повтор ради видео. Кнопка, молча звонящая голосом, выглядит
поломкой ровно один раз — а потом ей перестают верить.

## 6. Развилки

**~~Ж-В1. Две кнопки, долгое нажатие или карточка?~~ — решено 2026-09-23: две рядом.**
Цена в четверть ширины строки принята; она честнее невидимого долгого нажатия, о котором
узнают, только если рассказали.

**~~Ж-В2. Групповые звонки в журнале?~~ — решено 2026-09-23: не показываем, и показывать
нечего.**

У группового звонка **своя кнопка**, и она заглушка: в `GuestPageScreen` у неё намеренно
нет обратного вызова, нажатие открывает объяснение «позже». То есть **группового звонка в
клиенте не существует вовсе** — ручки сервера (`POST /calls/group`, `/join`) есть с самого
начала, а звать их некому.

Значит журналу нечего показывать, и первая редакция берёт только личные звонки
(`type = direct`). Групповые придут в журнал **вместе с самим групповым звонком**, не
раньше: строка о звонке, которого нельзя совершить, — обещание, а не история.

**~~Ж-В3. Счётчик пропущенных.~~ — решено 2026-09-23: отдельный.**

Отдельный от непрочитанных сообщений, а не общий: «три непрочитанных» и «три пропущенных
звонка» — разные срочности и разные действия. Сложенные в одно число, они означают «что-то
есть», то есть не означают ничего.

**~~Ж-В4. Срок хранения журнала.~~ — решено 2026-09-23: на сервере не удаляем вовсе.**

`calls_journal_days = 0`, и ноль означает «не убирать»; уборщик при нуле задачу
пропускает. Строка политики остаётся, чтобы исключение было видно в таблице сроков рядом
с прочими, а передумать можно было правкой строки.

**Сначала я выбрал 180 дней и обосновал их соседством с перепиской. Это был ответ на
неверный вопрос.** Верный — «а почему вообще удаляем». В строке звонка **нет ничего,
кроме метаданных**: кто, кому, когда, чем кончилось. Стирать там нечего.

А у нас на этот счёт есть правило, записанное и охраняемое тестом:

> `server/internal/store/retention.go:157` — «Строка остаётся: метаданные (кто, кому,
> когда, какого типа) не удаляются никогда — это отдельная плоскость с отдельным сроком».
> Тест: `api/retention_test.go:174`.

У сообщения стирается **содержимое**, строка живёт. Звонок из правила выпал по недосмотру:
уборка появилась 2026-09-20 с доводом «таблица растёт навсегда» — довод был про место, а
решение вышло про метаданные. Стирает звонки по-прежнему ровно одно: стирание аккаунта
(`account_purge_days`), то есть ручной запрос человека.

**Сколько держит телефон — другое решение и другое место.** Настройка «Память и трафик»:
срок и число строк, что наступит раньше. Это не повторение серверного числа — на сервере
числа больше нет, — а решение про место на устройстве. Умолчание 365 дней или 1000 строк.

## 7. Чего этот план не делает

**Не трогает медиа и сигналинг.** Журнал читает то, что уже записано; на ход звонка он не
влияет никак.

**Не заводит запись разговоров** — ни звука, ни расшифровок. Речь только о строке «кто,
когда, чем кончилось».

**Не делает журнал источником правды о состоянии.** Идёт ли звонок прямо сейчас, спрашивают
у `GET /calls/{id}`, а не у списка: список — про прошлое.
