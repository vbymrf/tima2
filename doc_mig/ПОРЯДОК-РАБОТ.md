# Порядок работ

Три дорожки живут в трёх документах: сервер — [КАРТА-СЕРВЕРА.md](КАРТА-СЕРВЕРА.md),
клиент — [ДОРОЖНАЯ-КАРТА.md](ДОРОЖНАЯ-КАРТА.md), документация — [README.md](README.md).
Каждый говорит, **что** делать. Ни один не говорит, **что следующее**.

Этот файл говорит только очерёдность и правила переключения. **Содержания задач
здесь нет** — оно разошлось бы с первоисточником через неделю. Задача называется
идентификатором (`С1.1`, `К2`, `Д3`), а что она значит — смотреть там.

## Зачем это отдельно

Очерёдность была подразумеваемой («С1 → С2 → С3»), и подразумеваемое уже дважды
дало сбой:

- **С2.2 оказался пропущен** — гейт был «на потом», потому что менять код сервера
  никто пока не собирался. В результате CI зеленел, ничего не проверяя.
- **С1.3 оказался тем же, что Д3.** Одна работа названа дважды в двух документах;
  такое делают либо дважды, либо ни разу.

---

## Правила очерёдности

### 1. Гейт раньше первой правки кода сервера

**Ни одной правки в `server/` до того, как С2.2 зелёный.**

**Выполнено 2026-08-21 на `edcfa50`:** в CI идут все 118 тестов, и **любой пропуск
валит задачу**. До этого серверная задача делала только `go build` и `go vet`, а
тесты шли на одной машине — то есть зелёный CI ничего не означал.

Правило остаётся действующим, потому что оно про **следующий** такой случай: гейт
ставится до первой правки кода, а не когда правка понадобилась. И остаётся
требование к отчётам: прогон без названных чисел `RUN/PASS/FAIL/SKIP` — не
доказательство, потому что `go test` печатает `ok` и для пакета, где всё
пропущено. Ожидаемые числа — в тире `server-full`
([PROFILE.md](../.cursor/skills/adaptive-test-runner/project/PROFILE.md)).

### 2. Круг вопросов закрывается раньше правок

**Пока в [ВОПРОСЫ-К-ЗАКАЗЧИКУ.md](ВОПРОСЫ-К-ЗАКАЗЧИКУ.md) есть неотвеченные
вопросы — код не правим.** Введено 2026-08-21 заказчиком.

Причина видна на первом же круге: ответ на А1 отменил вариант «по-разному в личном
и публичном», ответ на А3 отменил зависимость от адресной книги, а поправка к тому
же А3 отменила мой вывод про два недоступных состояния. Каждая из этих отмен
обошлась бесплатно только потому, что кода ещё не было. Начни я со схемы реакций —
платил бы миграцией.

**У каждого ответа обязателен блок «что это меняет»:** таблицы и столбцы, маршруты,
события, работа на клиенте, номер миграции, зависимости от других вопросов. Решение
без названных следствий — это мнение, а не решение: проверить его нечем и оценить
нечем.

### 3. Раньше — то, чей выход нужен не мне

Моя работа последовательна, ответы заказчика — нет. Поэтому задача, из которой
получается вопрос или решение для другого человека, идёт **первой**: пока ответ
думается, я занят следующей. Обратный порядок оставляет обоих ждать.

### 4. Измерение раньше стройки

Задачи, которые ничего не меняют, а только измеряют (`С1.1`, `Д3`), стоят перед
теми, что меняют. Они дёшевы, переделываются без последствий и задают объём всего
последующего. Стройка по неизмеренному — это то, чем проект уже болел: код
Р2/Р4/Р5б «писался вслепую и ни разу не собирался».

### 5. На два фронта не работаем

Правило из [КАРТА-СЕРВЕРА.md §5](КАРТА-СЕРВЕРА.md): пока v2 не отправила первое
сообщение, формат и API — единственное заведомо верное. Менять контракт и строить
клиент одновременно означает, что упавшее сообщение перестаёт что-либо значить.

---

## Текущий порядок

| # | Задача | Откуда | Состояние | Почему здесь |
|---|---|---|---|---|
| — | **С1.1** — дельта по `интерфейс.md §11` | карта сервера | **закрыт 2026-08-21** | — |
| — | **С2.2 + С2.3** — гейт и записка про стек | карта сервера | **закрыт 2026-08-21** на `edcfa50`: в CI `RUN=118 PASS=118 FAIL=0 SKIP=0` | — |
| — | **круг вопросов** | ВОПРОСЫ-К-ЗАКАЗЧИКУ.md | **закрыт 2026-08-21**: 16 вопросов, все отвечены | — |
| **1** | **К2** — `messenger-crypto` на KMP | дорожная карта | **ЗАКРЫТ 2026-08-21 на `f8b6a04`**: JVM 85/0/0/0, iOS-симулятор 78/0/0/0 | **Гейт клиента.** Без сошедшихся байт в байт векторов на JVM и iOS всё дальнейшее пишется вслепую |
| **2** | **Д3** — сверка `05-api` и `data-model.md` с кодом | README | **закрыт 2026-08-21** | К3 пишет сеть **против этих документов**. Сверять их после — значит писать клиент по тексту, который может расходиться с сервером |
| **3** | **Д-правки по решениям круга** | README | **закрыт 2026-08-21** | Исчезающие убраны из шести мест (четыре из них — нарисованные макеты), `разделы.md` дополнены рубриками страницы, закрыты два подтверждённых расхождения: `module-boundaries.md §5` и FTS5 в `tech-stack.md` |
| **4** | **К3** — сеть, база, Outbox | дорожная карта | **разблокирована**: гейт К2 закрыт | Сюда же переехал `core-encryption` из К2.6 — вместе с первым потребителем |
| **5** | **К4** — срез: текстовое сообщение туда-обратно | дорожная карта | после К3 | Первый живой запрос. Он же — первая настоящая проверка решений круга |
| **6** | **С3**, потом **С4–С6** | карта сервера | после К4 | Решено 2026-08-21 (Г2): сначала личное общение до работающей v2. С3 делается по надобности клиента — `min_client` к К7, `nodes` в Bot API к К6 |

**Кто выкатывает — решено** (2026-08-21, Г1): выкатываю я по SSH. До первой выкатки
закрываются три вещи, названные в Г1: две инструкции по одному действию, отсутствие
тега `deploy/` на развёрнутом коммите, расхождение `/opt/tima` с ветвью. Это не
отдельный этап — это условие первой выкатки.

---

## С1.3 и Д3 — одна работа

Обе задачи означают: сверить `doc/05-api` и `doc/02-architecture/data-model.md` с
кодом и миграциями, у каждого документа выставить вердикт и дату.

**Действует Д3**, потому что дорожка Д — про документацию целиком, и там же лежат
два уже подтверждённых расхождения (`module-boundaries.md §5` про `text + entities`
и `tech-stack.md` про FTS5). `С1.3` в карте сервера остаётся строкой-ссылкой, чтобы
не потерялся вход, но своей работы больше не описывает.

---

## Социальный слой: вход только через сформированную задачу

**Рамка «MVP» снята 2026-08-21.** Она относилась к клиенту v1 и к времени до отказа
от совместимости; после решения строить клиент заново «объём для MVP» перестал быть
критерием. Где эта формулировка осталась в документах — вычищается.

Порядок входа в `С4`–`С6`:

1. `С1.1` даёт дельту: что из §11 на сервере есть, чего нет, что расходится.
2. Из дельты я формирую **список вопросов** — там, где интерфейс допускает больше
   одного прочтения, а цена прочтений разная.
3. Заказчик отвечает **или переформулирует вопросы**: список — это база, а не
   анкета. Неверно заданный вопрос дороже незаданного, потому что на него отвечают.
4. Только после этого `С4`.

Причина такого входа: социального слоя на сервере нет **вовсе**, то есть каждое
решение здесь — не правка, а первый вариант. Первый вариант живёт дольше всех
остальных.

---

## Как меняется этот файл

Порядок переписывается целиком, когда меняется, — но **с записью в журнал ниже**.
Порядок без истории выглядит как всегда бывший таким, и тогда непонятно, какие
решения он уже пережил.

| Дата | Что изменилось | Почему |
|---|---|---|
| 2026-08-21 | Д3 и Д-правки закрыты; К2 сделан, гейт в проверке. Следующая — К3 | Сверка Д3 дала числа, ради которых она и делалась: каталог API описывал 26 маршрутов из 71 и помечал реализованными 6; модель данных молчала о 13 живых таблицах; в событиях WS нашлось расхождение устройства — документ отправляет `typing` и `receipt` кадрами, а код принимает их по REST |
| 2026-08-21 | Файл заведён. Порядок: С1.1 → Д3 → С2.2/С2.3 → развилка | Очерёдность была подразумеваемой и дала два сбоя: пропущенный гейт и задача, названная дважды |
| 2026-08-21 | Добавлено правило 2: круг вопросов закрывается раньше правок кода. Порядок стал С1.1 → вопросы → Д3 → развилка; С2 закрыт вне очереди, потому что понадобился под перенос сроков уборки | Три моих вывода были отменены ответами заказчика, и все три отмены обошлись бесплатно только потому, что кода ещё не было |
| 2026-08-21 | Круг закрыт, развилка разрешена ответом Г2: **сначала личное общение до работающей v2**. Новый порядок — К2 → Д3 → К3 → К4 → С3 и дальше. Серверная дорожка встаёт на паузу до К4 | Ни одна ручка социального слоя сейчас не проверяется приложением, потому что приложения нет. Первый вариант схемы живёт дольше всех остальных, и переделывать его пришлось бы уже с данными |

## Решение 2026-08-21: сервер обновляется, а не переустанавливается

К стенду для К4 идут за двумя вещами: накатить `0030_delivery_retention.sql` и
забрать публичный ключ подписи анклава, чтобы зашить его в клиент. Ни то, ни другое
не требует стирания.

Ключ подписи печатается при **каждом** старте анклава и не меняется между
перезапусками — сид лежит в его state-файле. Однократная выдача — это про доли
Шамира, а не про ключ подписи, и спутать их дорого: новый том `escrow-data`
обесценивает прежние пять долей, которые выдаются один раз.

Подробности и три состояния сервера, в одном из которых он окажется, —
[ПЕРЕУСТАНОВКА-СЕРВЕРА.md § «Обновление, а не переустановка»](ПЕРЕУСТАНОВКА-СЕРВЕРА.md#обновление-а-не-переустановка).

Отсюда порядок: обновить сервер → забрать ключ из журнала анклава → зашить в
`EscrowTrust.enclaveSigningPub` → только тогда клиент умеет отправлять. До этого
отказ закрытый: непроверенный ключ эпохи не принимается.

## Решение 2026-08-22: стенд переводится на ветку `stand` ПОСЛЕ интерфейса

Сейчас чекаут `/opt/tima` на стенде стоит на `feature/device-link-qr`, и работающий
сервер собран из неё 14 августа. Рабочая ветка проекта — `stand`, то есть расхождение
есть и его надо свести.

Но не сейчас, и вот почему. Для К4 обновление не потребовалось: путь сообщения на
стенде тот же, под который написан клиент v2 — измерено, сквозной путь пройден и с ПК,
и с телефона. А переключение ветки переводит серверный код на текущую линию **под
живыми клиентами v1**, которые стоят у тестировщиков. Там по-настоящему проверяется
правило «сервер обязан работать со старым клиентом», и делать это походя между делом
нельзя.

Поэтому: сначала интерфейс, потом отдельная задача «перевести стенд на `stand`», и в
неё входит

- сохранение настроек, живущих только на машине —
  [поломка LiveKit](ПЕРЕУСТАНОВКА-СЕРВЕРА.md#поломка-livekit-при-переключении-ветки);
- прогон клиента v1 против нового сервера;
- перевод настроек LiveKit на переменные окружения, чтобы ловушка не возвращалась.
