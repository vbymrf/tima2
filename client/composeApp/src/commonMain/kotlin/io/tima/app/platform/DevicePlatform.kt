package io.tima.app.platform

/**
 * Платформа этого устройства для сервера: `android` / `ios` / `desktop`.
 *
 * Нужна ровно для одного правила: подтверждать привязку нового устройства по QR
 * вправе только телефон (`key-lifecycle.md` §2). Значение самообъявленное — до
 * аппаратной аттестации сервер проверить его не может, поэтому это правило
 * порядка, а не граница безопасности.
 */
expect fun devicePlatform(): String
