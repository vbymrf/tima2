package io.tima.app.platform

/**
 * Звуки вызова: рингтон входящего и гудок исходящего. Без них звонок «идёт молча» —
 * непонятно, дозваниваешься ли ты и зовёт ли тебя кто-то.
 * [incoming] = true — рингтон и вибрация; false — гудок «ту-ту» в трубке.
 */
expect fun startRinging(incoming: Boolean)

/** Остановить звуки вызова: сняли трубку, отклонили или разговор завершён. */
expect fun stopRinging()
