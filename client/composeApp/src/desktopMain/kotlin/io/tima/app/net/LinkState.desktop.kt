package io.tima.app.net

/**
 * На ПК подписки на смену сети нет — и городить опрос интерфейсов ради неё не
 * стоит: настольная машина живёт на одном соединении, а пауза в две минуты здесь
 * не так заметна, как на телефоне в дороге.
 */
actual fun observeNetworkChanges(onChange: () -> Unit) = Unit
