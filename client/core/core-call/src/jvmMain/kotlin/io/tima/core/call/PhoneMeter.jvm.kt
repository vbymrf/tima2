package io.tima.core.call

/**
 * ПК: стенда здесь нет.
 *
 * Испытания идут на телефонах — там `livekit-android`, там кодеры, там нагрев. Настольная
 * машина не ответит ни на один вопрос забега: её процессор к телефонному отношения не
 * имеет, а звонков на ней пока нет вовсе (К7.1).
 *
 * `null`, а не нули: «ноль процессора» и «мерить нечем» читаются одинаково только в коде,
 * а в отчёте это разные утверждения.
 */
actual fun phoneLoad(): PhoneLoad? = null

actual fun phoneTraffic(): PhoneTraffic? = null
