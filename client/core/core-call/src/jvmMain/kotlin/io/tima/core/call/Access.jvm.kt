package io.tima.core.call

/**
 * ПК: спрашивать некого и нечего.
 *
 * Движка звонков на настольной системе пока нет (К7.1, маршрут не выбран), а системного
 * разрешения на микрофон там не существует вовсе. `true` здесь — не «разрешено», а «этот
 * шаг к делу не относится»: звонок остановится дальше и по своей причине, а не соврав про
 * отказ в разрешении, которого никто не просил.
 */
actual fun askCallAccess(video: Boolean, onResult: (Boolean) -> Unit) = onResult(true)
