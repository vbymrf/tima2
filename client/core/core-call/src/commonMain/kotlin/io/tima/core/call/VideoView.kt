package io.tima.core.call

/**
 * Дорожка видео, которую можно показать, — **непрозрачная ручка**.
 *
 * ── ПОЧЕМУ НЕПРОЗРАЧНАЯ ─────────────────────────────────────────────────────
 *
 * Картинка рисуется платформенной поверхностью: на Android это `SurfaceViewRenderer` из
 * libwebrtc, общего Compose-вида у неё нет и не будет. Экран звонка при этом общий — один
 * и тот же код на телефоне и на ПК.
 *
 * Развязывает их эта ручка: экран знает, что дорожка **есть**, и просит платформу сделать
 * под неё вид. Чем дорожка является на самом деле, общий код не знает — и потому не
 * ломается, когда под ним сменят движок.
 *
 * ── ПОЧЕМУ ИНТЕРФЕЙС, А НЕ `expect class` ───────────────────────────────────
 *
 * `expect class` без тела требует от реализации безаргументного конструктора, а нашей
 * ручке нужны и комната, и дорожка: поверхности LiveKit отдаёт контекст EGL, общий на всю
 * комнату, и без него она показывает чёрное молча. Интерфейс это требование снимает, а
 * непрозрачности не теряет — снаружи он всё так же пуст.
 *
 * Заодно у ПК и iOS не заводится ни строчки: реализации там просто нет, как нет и звонков.
 */
interface VideoHandle
