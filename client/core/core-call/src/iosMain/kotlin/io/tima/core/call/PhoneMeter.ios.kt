package io.tima.core.call

/**
 * Apple: приложения нет, и стенда тоже.
 *
 * Таргеты в библиотеках есть, самого приложения — нет (ВЕРСИИ/КЛИЕНТЫ.md). Числа отсюда
 * некому ни снять, ни прочитать.
 */
actual fun phoneLoad(): PhoneLoad? = null

actual fun phoneTraffic(): PhoneTraffic? = null
