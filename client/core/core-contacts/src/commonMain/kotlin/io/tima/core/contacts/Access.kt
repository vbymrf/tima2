package io.tima.core.contacts

/**
 * Спросить доступ к телефонной книге — ПЛАН-КОНТАКТОВ.md, Д3.
 *
 * **Спрашивается при открытии вкладки, а не при запуске.** Приложение, просящее доступ к
 * контактам на первом экране, не может объяснить зачем: человек ещё не видел ни одного
 * контакта и отклонит — и правильно сделает.
 *
 * **Диалог показывается один раз за установку** (и на Android, и на Apple). Потратить его
 * на запуск приложения значит потратить впустую: второго не будет, и дальше человеку
 * придётся идти в системные настройки.
 *
 * @param onResult `true` — доступ дали. `false` означает и «отказал», и «спросить
 *   больше нельзя», и «на этой платформе спрашивать нечего»: экран во всех трёх случаях
 *   делает одно и то же — показывает, что контакты не прочитаны, и предлагает добавить
 *   вручную.
 */
expect fun askContactsAccess(onResult: (Boolean) -> Unit)
