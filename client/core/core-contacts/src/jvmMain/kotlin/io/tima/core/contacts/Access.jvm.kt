package io.tima.core.contacts

/**
 * ПК: спрашивать нечего.
 *
 * Телефонной книги у настольной системы нет, значит нет и доступа к ней. Ответ `false`
 * здесь не отказ, а «такого разрешения не существует», и экран говорит именно это:
 * «контакты добавляют вручную», а не «разрешите доступ».
 */
actual fun askContactsAccess(onResult: (Boolean) -> Unit) = onResult(false)
