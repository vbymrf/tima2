<?xml version="1.0" encoding="utf-8"?>
<!--
  Манифест каркаса. Разрешений нет ни одного: они появляются вместе с тем, что их
  требует (камера — в К5 с QR, микрофон — в К7 со звонками). Разрешение, выданное
  заранее «чтобы потом не забыть», объяснить пользователю нельзя.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="TIMA v2"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
