# План рефакторинга: хранение сообщений, escrow, личность, звонки

> **Статус:** рабочий план, не канон. Обновляется по ходу работ.
> Заведён и сведён 2026-07-30. Лежит в корне, а не в `doc_add`: тот вне гита, а план
> терять нельзя. Пользовательская формулировка — [ДОКУМЕНТАЦИЯ/](ДОКУМЕНТАЦИЯ/),
> канон — `doc/`.
>
> **Этапы названы Р0…Р6**, чтобы не путать с фазами продуктовой дорожной карты
> ([ФАЗА-1-СТАТУС.md](ФАЗА-1-СТАТУС.md), `doc/01-product/roadmap.md`) — там своя
> нумерация и про другое.

## 0. Зачем

Три требования, вокруг которых собран весь рефакторинг:

1. **Метаданные храним всегда и в открытом виде.**
2. **Персональные данные скрываем.**
3. **Ключи живут ограниченный срок, и срок — настройка, а не константа.**

Отсюда главная идея: **разделить сроки жизни трёх вещей, склеенных сейчас в одной
строке таблицы.**

| Плоскость | Что | Срок | Видимость |
|---|---|---|---|
| Метаданные | кто-кому-когда-какого типа | вечно | открыто, но псевдонимно |
| Контент | текст, медиа | пока жив нужный ключ | шифртекст |
| Личность | телефон ↔ идентификатор, имя | пока живёт аккаунт | под ключом |

Связывает крипто-шреддинг: истёк срок ключа — контент недоступен, метаданные целы.

## 1. Принятые решения

| № | Решение |
|---|---|
| 1 | Модель угроз для персональных данных — **утечка базы или бэкапа**, не компрометация хоста |
| 2 | Escrow — **только для ордеров**. Пользователю недоступен; потерял ключи — потерял переписку |
| 3 | Удаление пользователем — **пометка**, не стирание. Физически стираем, когда аккаунт удалён / чат в архиве **и** уничтожен escrow-ключ периода |
| 4 | Escrow-ключ — на пару **«чат × эпоха»**, генерируется лениво при первом сообщении чата в эпохе |
| 5 | Срок ключа = длина эпохи + срок по закону = **1 месяц + 6 месяцев = 7 месяцев** |
| 6 | Состояния аккаунта: **временный / постоянный / архивный**. Постоянный по неактивности не удаляется никогда |
| 7 | Аккаунт — **цепочка идентификаторов**. Текущий тот, чьи ключи доказуемы |
| 8 | Связка идентификаторов без подписи прежним ключом — собеседник видит **предупреждение о смене личности** |
| 9 | **Key-commitment делаем**, конверт версии 2 |
| 10 | Звонки: **в работу LiveKit не вмешиваемся**. Реализуем только групповые звонки |
| 11 | Ключ персональных данных — **отдельно от базы**, за одним интерфейсом. Отдельный сервер — позже |
| 12 | Содержимое сообщения — **массив узлов + разметка**, без якорей и смещений; разметка приватного шифруется; блоки в обоих контурах, профили возможностей обязательны ([ADR-0011](doc/adr/0011-message-content-model.md)) |
| 13 | Данные текущей базы **выбрасываемые**: база пересоздаётся с нуля, сохранять и переносить нечего |

### Сроки — все настройки, не константы

| Обозначение | Смысл | Сейчас |
|---|---|---|
| У | Неактивность временного аккаунта → архив | 30 дней |
| СУ | Архив аккаунта → стирание | 30 дней |
| К | Окно воссоединения истории | 30 дней (**К ≤ СУ**) |
| Н | Архив чата → пометка удалённым | 180 дней |
| СЧ | Пометка чата → стирание | 180 дней |
| — | Длина эпохи escrow | 1 месяц |
| — | Срок жизни escrow-ключа | 7 месяцев |
| — | Срок хранения по закону | 6 месяцев |
| — | Окно восстановления связи в звонке | 30 секунд (в конфигурации LiveKit) |

### Номера ADR

| Номер | Решение | Статус |
|---|---|---|
| 0011 | Модель содержимого сообщения | принят |
| 0012 | Escrow: ключи эпох и сроки | принят |
| 0013 | Key-commitment и конверт версии 2 | принят |
| 0014 | Цепочка идентификаторов | принят |
| 0015 | Сроки хранения и уничтожение данных | не написан |

## 2. Швы, которые режем сразу

Дёшевы сейчас, дороги потом. Не требуют ни второго сервера, ни шардирования.

1. **`key_id` рядом с каждым шифртекстом** — иначе первая же ротация упрётся в «каким ключом это зашифровано». ✅ сделано в Р1а.
2. **Регион в реестре escrow-ключей**, пусть значение пока одно.
3. **`chat_id` как единственный ключ разбиения** — ни одна функция не должна требовать запроса по всем чатам сразу.
4. **Ключи только за интерфейсом или по адресу** — у escrow уже так (`ESCROW_URL`), у персональных данных сделано в Р1а.
5. **Миграции остаются append-only.** База пересоздаётся, но правку прошлых миграций не делаем: через несколько недель появятся живые данные, и привычка «поправить старую миграцию» станет опасной ровно тогда, когда её уже не отследить.

## 3. Найденные нестыковки и как разрешены

Сверка сведённого плана, 2026-07-30.

| Нестыковка | Разрешение |
|---|---|
| **Р1а закоммичена, но не выкачена.** В бою последняя миграция `0016`, 6 пользователей, 9 сообщений | Данные выбрасываемые (решение 13): база пересоздаётся с нуля, backfill на живых данных не нужен |
| **`BackfillPII` и двухрелизный переход** проектировались, чтобы не потерять существующие строки | При пересоздании базы переносить нечего. Backfill удалён, открытые колонки убирает `0018` сразу |
| **«Эпоха × регион × шард» против «ключ на чат × эпоха»** — выбирали одно, уточнили до другого | Ключ на чат **тоньше** шарда и его отменяет. Реестр — `(регион, эпоха, chat_id)`. Шард, если понадобится, останется только операционной группировкой, а не границей изоляции |
| **«Р3 и Р5б выпускать вместе, потому что обе трогают формат»** | Неточно. Key-commitment меняет подписываемые байты конверта; узлы и разметка живут **внутри** зашифрованной нагрузки и добавляются аддитивно. Технически независимы; вместе — только чтобы сэкономить цикл выкатки клиента |
| **Имя «Фаза 1» занято** продуктовой дорожной картой | Этапы рефакторинга названы Р0…Р6 |
| **`phone_bidx` нельзя делать `NOT NULL`**, хотя на чистой базе соблазн есть | Временные аккаунты заводятся **без телефона** (`key-lifecycle.md` §7, решение 6). Ограничение пришлось бы снимать через две недели. Колонки остаются nullable, уникальность — частичным индексом |
| **Уникальность номера** сейчас `WHERE phone_bidx IS NOT NULL`, по документации нужна `WHERE deleted_at IS NULL` | Меняется в Р4, когда появится состояние «архивный». Условие записано в комментарии `0017` |
| **Защита `ResetForTests`** требует суффикса `_test`; боевая база называется `tima` | Совпадений нет, защита работает. Проверено |

## 4. Этапы

### Р0 — развязать канон ✅ выполнено

- [x] ADR-0006 Поправка-1: все звонки через SFU, качество медиа — зона LiveKit
- [x] `calls-livekit.md` §2 и §5 приведены к коду
- [x] Аудит Kodium: пометка, что ADR-0016/0017 не существуют
- [x] Бриф по звонкам переписан в согласованном виде

### Р1а — персональные данные в покое ✅ выполнено

- [x] `internal/pii`: шифрование значений и слепой индекс, ключ файлом вне базы
- [x] Миграция `0017`: `phone_bidx` / `phone_enc` / `name_enc`
- [x] Восемь функций хранилища переведены на шифртекст и индекс
- [x] `ResetForTests` отказывается работать с базой без суффикса `_test`
- [x] Отдельный том `pii-data`; тестовый PostgreSQL на сервере
- [x] Миграция `0018`: убраны открытые `phone` и `display_name`
- [x] `BackfillPII` убран — переносить нечего
- [x] Боевая база пересоздана с нуля, выкачено 2026-07-30: 18 миграций, в `users` открытых колонок нет
- [x] Каталог ключа создаётся в образе под UID 10001 — свежий том иначе root-овый и процесс падает

### Р1б — цепочка идентификаторов ✅ выполнено

- [x] `persons` как аккаунт; `users` стал «личностью в период» (миграция `0019`)
- [x] `person_id`, `valid_from` / `valid_to`, `linked_from`, `link_proof`
- [x] Телефон и имя переехали в `persons`: профиль принадлежит аккаунту, а не личности
- [x] `POST /users/identities` — разрешение «идентификатор → аккаунт» для клиентов
- [x] Различение доказанной и административной связки
- [x] `StartNewIdentity` — примитив под перерегистрацию и воссоединение
- [x] Инвариант «одна текущая личность на аккаунт» частичным уникальным индексом
- [x] [ADR-0014](doc/adr/0014-identity-chain.md)

Состояния аккаунта (временный/постоянный/архивный) и сроки — этап Р4.

Пользователю не видно ничего. Фундамент под Р4.

### Р2 — escrow-эпохи (осталась подпись конфига)

**Анклав:**
- [x] Связка ключей: ключ на пару «чат × эпоха», хранится отдельным файлом
- [x] **Независимые seed'ы** — никакой деривации из общего корня
- [x] Ленивая генерация: ключ появляется при первом обращении к области
- [x] Уничтожение по сроку: физическое удаление файла + запись в аудит
- [x] Автоматический проход уничтожения по таймеру, а не по кнопке
- [x] `POST /v1/key`, `GET /v1/keys/{id}`, `POST /v1/unseal-scoped`
- [x] Ордер со scope: наружу только ключи сообщений, приватный ключ — никогда
- [x] Срок ключа = конец эпохи + срок по закону (покрывает и последний день эпохи)

**Бэкенд:**
- [x] Реестр `escrow_keys` (миграция `0020`): публичные части + `destroy_at` для гейта Р4
- [x] `GET /escrow/key?chat_id=` — ключ области, лениво через анклав, с кэшем в реестре
- [x] Перекрытие эпох: у границы отдаётся и следующий ключ
- [x] Регион как измерение, значение пока одно
- [x] [ADR-0012](doc/adr/0012-escrow-key-epochs.md)

**Клиент:**
- [x] Выбор ключа по чату вместо legacy-ключа; кэш до конца окна валидности
- [x] Групповой ключ депонируется на ключ эпохи своей группы
- [ ] Подпись конфига анклавом и проверка клиентом (иначе компрометация бэкенда даёт подмену ключа)
- [ ] Валидация `escrow_key_version` на приёме — после того, как все клиенты перейдут

Формат конверта не ломается: `escrow_key_version` становится суррогатным идентификатором записи реестра.

### Р3 — конверт версии 2 ✅ выполнено (осталась выкатка клиента)

- [x] `key_commitment = HKDF-SHA256(message_key, "tima/commit/v1")`
- [x] В подписываемые байты хвостом; раскладка версии 1 не тронута
- [x] Сверка при получении: ключ добыт любым путём — обязательство пересчитывается
- [x] Сервер требует наличия и длины при версии ≥ 2 (значение проверить не может)
- [x] Отказ от неканонической подписи Ed25519 (low-S) на приёме
- [x] KAT-вектор `canonical_bytes_v2`; Go и Kotlin сходятся байт-в-байт
- [x] Приём версии 1 сохранён на время перехода
- [x] [ADR-0013](doc/adr/0013-key-commitment.md)
- [ ] Выкатка: **сервер раньше клиента**, обратный порядок сломает отправку

Групповые сообщения обязательства пока не несут: у них другой preimage и один GK
на версию, а не ключ на сообщение. Отдельная задача.

### Р4 — удаление и сроки (сервер готов, остался клиент)

**Сервер:**
- [x] Состояния аккаунта: `temporary` / `permanent` / `archived`, `deleted_at`, `purge_after`, `last_active_at`
- [x] Уникальность номера — только среди неархивных: архивный аккаунт номер не держит
- [x] Сроки — таблица `retention_policy`, а не константы в коде
- [x] Стирание содержимого с гейтом по `destroy_at` эпохи; метаданные остаются
- [x] Воркер: архивация неактивных временных аккаунтов и стирание содержимого
- [ ] ADR-0015

**Клиент (осталось):**
- [ ] «Удалить аккаунт» в настройках
- [ ] Экран выбора при занятом номере: перенос против «начать заново»
- [ ] «Присоединить прежний аккаунт» по QR в окне воссоединения

**Открытый вопрос:** архив ЧАТА не реализован. Для личных чатов нет таблицы —
`chat_id` вычисляется из пары участников, и непонятно, архив это состояние чата
целиком или у каждого свой. Решение нужно до реализации.

Зависело от Р1б (состояния на `persons`) и Р2 (гейт по уничтожению ключа) — оба готовы.

### Р5 — групповые звонки (сервер готов, остался клиент)

- [x] Участники звонка как сущность (миграция `0023`, `call_participants`)
- [x] `POST /calls/group` — приглашаются все участники группы
- [x] `POST /calls/{id}/join` — один эндпоинт на первый вход и на возвращение
- [x] Для звонка один на один повторного входа нет
- [x] **Обработчик вебхуков LiveKit** с проверкой подписи и сверкой хэша тела
- [x] `RoomService` по HTTP: «завершить звонок» действительно закрывает комнату
- [x] `livekit.yaml`: TURN включён, умолчания комнаты заданы
- [ ] UI: сетка участников, состояния звонка
- [ ] Проверка через `livekit-cli` (двумя телефонами группу не проверить)

Работы с качеством медиа нет — это зона LiveKit.

**Решение по SDK (одобрено):** официальный `server-sdk-go` не берём. Измерено:
только `livekit/protocol` компилирует 423 пакета против 346 у всего нашего бэкенда.
RoomService — это twirp поверх HTTP, и нам из него нужны два вызова.

### Р5б — модель содержимого сообщения

Решение — [ADR-0011](doc/adr/0011-message-content-model.md).

- [ ] `MessageBody`: поля `nodes` и `markup` новыми номерами, аддитивно
- [ ] Переходный релиз: отправитель кладёт и плоский `text`, и новую структуру
- [ ] Стабильные идентификаторы узлов в разметке, не позиция в массиве
- [ ] Публичный контур: `channel_posts.text` → `nodes TEXT[]` + `markup JSONB`
- [ ] Метаданные: основные — колонки закрытым списком, редкие по типам — ограниченный JSONB
- [ ] Редактор: один на оба контура
- [ ] Профили возможностей: пузырь сообщения и публикация
- [ ] Каскад стилей: фиксированный порядок + три режима читателя + защитный пол
- [ ] Кеш рендера с ключом по версии стилей, теме, ширине, масштабу шрифта
- [ ] Тест-векторы формата

Основная стоимость — не формат, а отрисовка блоков на всех платформах.

### Р6 — документация

- [x] ADR-0011
- [ ] ADR-0012…0015 по мере принятия этапов
- [ ] `ДОКУМЕНТАЦИЯ/01-сервер` и `03-шифрование`
- [ ] `doc/03-security/*` под эпохи и цепочку идентификаторов
- [ ] `doc/02-architecture/data-model.md` под `persons`

Идёт хвостом за каждым этапом, а не в конце.

## 5. Порядок

```
Р1а → Р1б → Р4
        ↑
Р2 ─────┘
Р3   независим, выпускать вместе с Р5б ради одного цикла клиента
Р5   независим, можно параллельно
```

Р1а и Р1б закрыты. Ближайший шаг — **Р2** (escrow-эпохи): без него Р4 не начать,
потому что гейт стирания опирается на уничтожение ключа эпохи.

## 6. Открытые вопросы

| Вопрос | Блокирует |
|---|---|
| Что делать, если у постоянного аккаунта перевыпущен номер оператором | Р4 |
| Чей идентификатор главный после воссоединения (предложение: новый) | Р4 |
| Нужен ли ручной вход в групповой звонок | Объём Р5 |

## 7. Рядом

- [ДОКУМЕНТАЦИЯ/04 — удаление аккаунтов и чатов](ДОКУМЕНТАЦИЯ/04-данные-и-удаление/удаление-аккаунтов-и-чатов.md)
- [ДОКУМЕНТАЦИЯ/02 — перенос аккаунта на новое устройство](ДОКУМЕНТАЦИЯ/02-приложение/перенос-аккаунта-на-новое-устройство.md)
- [ADR-0011 — модель содержимого сообщения](doc/adr/0011-message-content-model.md)

Вне гита, только на рабочей машине (`doc_add/` в `.gitignore`):
`call-media-reconnect-doc-gaps.md`, `kodium-security-audit.md`, `message.docx`.
