# ADR-0025: События звонка — кто кому что говорит

**Статус:** принят · **Дата:** 2026-09-20

## Контекст

Звонки заработали живьём 2026-09-19, и за два дня живых прогонов нашлось девять поломок.
**Семь из девяти — не в медиа, а в событиях**: кто-то что-то сказал, а слушатель не
услышал. Медиа при этом работало с первого дня.

Разбор каждой упирался в один вопрос: *где именно потерялось слово?* Отвечать на него
приходилось каждый раз заново, потому что потоков событий **четыре**, они разные по
природе, и нигде не были перечислены рядом:

| Поток | Кто читает | Живёт |
|---|---|---|
| лента окна 0 | человек | пока идёт звонок |
| журнал телефона | тот, кто разбирает | сутки, уезжает в отчёт о проблеме |
| кадры живого канала | код клиента | мгновение |
| состояние звонка в базе | сервер | всегда |

Путать их дорого. «Событие есть» в одном потоке ничего не говорит про три остальных: в
ленте может стоять «Собеседник положил трубку», а в базе звонок числиться идущим.

Две поломки из семи — **прямое следствие того, что перечня не было**:

- **`missed` не разбирался.** В клиенте стоял перечень конечных состояний «`declined`
  или `ended`». Неотвеченный звонок сервер закрывает словом `missed`; клиент его
  выбрасывал, и звонящий слушал «Звоним…» без конца. При этом `declined` звонки не шлют
  вовсе — это слово из другого места сервера. Перечень был неверен с обеих сторон сразу.
- **`call.participant_left` не разбирался вовсе** и уходил в «незнакомый кадр». Так
  кончается звонок, у которого вторую сторону убили или она потеряла сеть насовсем:
  нажатия не было, и `call.state ended` не придёт.

## Решение

### 1. Четыре потока перечислены и разделены по назначению

**Лента окна 0 — только то, чего человек не увидит сам.** Включённый микрофон виден по
кнопке; «собеседник показывает себя, а вы нет» по экрану не читается. События копятся
списком, показываются по одному (решение заказчика 2026-09-20): в строке последнее,
«развернуть» открывает целиком, «далее» и «назад» листают остальные.

**Журнал — то, чего не хватит потом.** Правило состава: в журнал идёт каждый шаг, по
которому потом придётся отличать «не дошло» от «дошло и не сработало». Этого правила не
было, и немой звонок оказался невидим: журнал знал «звонок начат» и «звонок закончен», а
поднялась ли дорожка микрофона — не знал никто. Причину нашли по **расстоянию между
строками**, а не по строке.

**Кадры канала — слово сервера, без нашего перечня.** См. решение 2.

**Состояние в базе — четыре слова:** `ringing` · `answered` · `ended` · `missed`.

### 1а. Устройство говорит только о себе, и только когда доступно

**Главное ограничение всей этой машины**, названное заказчиком 2026-09-20 после того, как
из-за его нарушения звонки встали у всех.

Из него следуют три правила, и каждое нарушалось:

**Состояние человека знает только его устройство.** «Занят», «показываю видео», «микрофон
включён» — это про устройство, и сказать их вправе оно одно. Сервер такое состояние не
наблюдает, он лишь хранит то, что ему сказали.

**Молчание устройства — не сведения.** Не ответило — это не «свободен» и не «занят», это
«не знаем». Вывод из молчания всегда будет догадкой, и догадка тем вреднее, чем дольше
живёт.

**Всё, во что сервер верит про человека, обязано истекать само.** Устройство может
исчезнуть навсегда — приложение убили, телефон утонул, — и тогда закрыть сказанное им
некому. Запись, которую закрывает только внешнее событие, однажды останется открытой.

#### Чем это стоило

Проверка «занят» на сервере считала занятым и того, у кого в базе висело состояние
`answered`. Оно закрывается снаружи и само не истекает. Один незакрытый звонок пятичасовой
давности сделал обоих участников недоступными: **позвонить не мог никто**.

Окно было в четыре часа — и тем цена утечки была назначена в четыре часа. Комментарий,
написанный тогда же, утверждал, что «ошибка длиной в четыре часа заметна и чинится сама».
Заметна оказалась, сама не починилась.

#### Как устроено теперь

| Что | Кто говорит | Что будет, если промолчит |
|---|---|---|
| «мне звонят» | сервер, он сам завёл звонок | истекает за две минуты |
| «я занят другим разговором» | **устройство**, закончив вызов с причиной `busy` | звонящий услышит «никто не ответил» — не ложь, а «не знаем» |
| «я ответил на другом устройстве» | сервер, по факту ответа | соседи звонят до своего срока |
| «комната жива» | **LiveKit**, на прямой вопрос | уборщик не трогает строку: ошибка в сторону «жив» |

Последняя строка — единственное состояние, которое сервер проверяет **сам**, и потому она
единственная, на которую он вправе опираться, закрывая чужие записи.

### 2. Конец звонка определяется от обратного

**Продолжается только `answered`. Всё остальное — конец.**

Перечень конечных состояний обещал бы, что мы знаем их все. Сервер волен завести новое
слово, и молчание клиента будет тем же самым — неотличимым от поломки. Обратное правило
ошибается в безопасную сторону: незнакомое слово кончит звонок, а не подвесит его.

Это продолжение правила, заведённого при разборе кадров звонка: **своего перечня
состояний клиент не держит** — «свой перечень обещал бы, что мы перечислили все случаи».
Перечень всё-таки завёлся, в другом месте и тихо.

### 3. У звонка есть срок ответа, и стоит он на клиенте

**Сорок пять секунд** — столько же, сколько звонит обычный телефон.

Правильное место — сервер: он переживёт убитое приложение и скажет обоим разом. Но там
нужен отложенный обход звонков, которого нет, а API менять сейчас нельзя
([Plan.md §0.0](../../doc_mig/Plan.md), решение 5: формат конверта и ручки стоят, пока v2
не заработает). На клиенте это один `delay` и уже существующий `/end`, который сервер
понимает как `missed` и рассылает второму.

**Срок стоит у обеих сторон и одним числом.** Разойдись числа, одна сторона гасила бы
звонок, который у другой ещё идёт.

Когда у сервера появится отложенная работа, срок переезжает туда, а клиентский остаётся
страховкой на случай, когда сервер не ответил.

### 4. Клиент разбирает всё, что сервер шлёт про звонок

| Кадр | Когда шлёт сервер | Что делает клиент |
|---|---|---|
| `call.incoming` | создан звонок | показать входящий |
| `call.state` `answered` | ответили | ничего: звонок продолжается |
| `call.state` — любое другое слово | завершён участником, комнатой или сроком | положить трубку |
| `call.participant_left` | вебхук LiveKit: сторона исчезла | положить трубку, **если ушёл собеседник** |
| `call.unreachable` | вызов не подтвердило ни одно устройство собеседника | строка в ленте; **трубку не класть** |

Предпоследняя проверка — не перестраховка. Сегодня звонок один на один, и уход
собеседника его кончает; в группе уход одного из пятерых разговора не кончит, и менять
придётся ровно это место.

Последняя строка — единственный кадр звонка, который **ничего не меняет в состоянии**.
`call.unreachable` говорит о связи, а не о человеке: устройство вернётся и заберёт вызов
из журнала само (`ws.go`, дожим), и до конца сорока пяти секунд телефон ещё зазвонит.
Поэтому кадр отдельный, а не слово в `call.state`, — тот по решению 2 кончил бы звонок.
Разбор: [ADR-0027 §5](0027-event-delivery.md).

## Последствия

**Незнакомый кадр звонка — теперь беда, а не мелочь.** Раньше он тихо уходил в «неизвестные»;
два таких кадра стоили двух дней разбора. Кадр, который сервер шлёт, а клиент не
разбирает, — такой же разрыв, как несовпавшая схема.

**Журнал звонка стал длиннее втрое**, и это цена, принятая сознательно: отчёт о проблеме
должен отвечать на вопрос «что вообще происходило», а не только «чем кончилось».

**Сорок пять секунд — число в одном месте** (`CallHost.RINGING_LIMIT_MS`). Не измеряется
ничем и меняется решением, а не подбором.

**Чего это не решает.** ~~Журнала звонков как хранимой вещи по-прежнему нет~~ — см.
Поправку-1. Входящий звонок при закрытом приложении не приходит вовсе — push-механизма
нет, и это отдельная работа.

---

## Поправка-1 (2026-09-23): журнал звонков есть, и был всё это время

Строка выше вводила в заблуждение, и правится вместе с появлением экрана.

**Про кадры и ленту она верна:** ни одно из перечисленных здесь событий действительно
никуда не ложится — это живые кадры, и складывать их в историю незачем.

**Про состояние в базе — нет.** Таблица `calls` пишется с первого дня звонков и держит
всё, что нужно строке журнала: кто кому, `kind`, состояние, `created_at`, `answered_at`,
`ended_at`. Оно ложится и живёт. Работа над вкладкой «Звонки» оказалась поэтому не
«завести журнал», а «дотянуть две ручки до экрана».

Вкладка «Звонки» заглушкой быть перестала. Как устроен журнал и почему источник правды у
него — сервер, в отличие от переписки: **[ADR-0026](0026-call-journal.md)**.

## Поправка-2 (2026-09-26): личный звонок — своя лента `cts`, журнал устройства не трогается

Решение заказчика, ПЛАН-ВХОДЯЩЕГО-ЗВОНКА.md ВЗ0а (вариант «б»).

**Что было не так.** С 23.09 (ПЛАН-ДОСТАВКИ-ПОЛОСАМИ, П2) вызов ехал подсказкой
`call.poke {call_id}` без номера события. Подтвердить её было нечем, курсор журнала не
двигался, а проверка «никто не забрал» (§1а) смотрела именно его. Любой звонок без ответа
за 5 с звонящий видел как «не в сети» — 2026-09-26 Redmi получил вызов за 2,2 с, а realme
через 8 с написал «не в сети», и трубку положили. Подтверждать подсказку номером журнала
нельзя: `ack` сдвинул бы курсор через ещё не забранные сообщения.

**Что стало.** У личного звонка своя переменная — `cts`, номер изменения звонков **на
человека**:

| | Сообщения | Личные звонки |
|---|---|---|
| Где лежат | журнал устройства (`device_events`) | `call_updates` — лента человека, живёт сутки |
| Подсказка | `sync.poke {event_id, pts, qts, seq}` | `call.poke {cts}` |
| «Пойди и забери» | `sync.pull` | `GET /calls/updates?after=N` — ответ составлен для спрашивающего устройства (`here`) |
| Подтверждение | `ack {event_id}` | `call.ack {cts}` — свой курсор `call_cursors`; номер выше вершины отвергается |

Изменения — словами: `ringing`, `delivered`, `unreachable`, `answered`, `declined`,
`cancelled`, `busy`, `ended`, `missed`, `seen`.

**Что меняется в решениях этого ADR:**

- **§1а остаётся по смыслу, меняется основание.** «Не в сети» по-прежнему наблюдение
  связи, а не состояние человека, и звонок от него не кончается. Но видит его сервер
  теперь по `call.ack`, а не по курсору журнала: подтвердило устройство — у звонящего
  `delivered` («Звонит»), не подтвердило за 5 с — `unreachable`.
- **§2 (конец от обратного)** — клиент по-прежнему кончает звонок на всём, кроме
  `answered`; слов стало больше, и `declined` / `missed` звонящему называются словами.
- **§3 (срок на клиенте)** — дополнен **серверным сроком**: звонок, не закрытый за 50 с,
  сервер закрывает сам и пишет `missed` обеим сторонам. Без этого «пропущенный» не рождался,
  если оба телефона были без связи: уборщик ходит раз в час.
- **`taken` исчезает как отдельное слово**: «ответили на другом устройстве» теперь
  `answered` с `here = false` в ответе ленты.

**Групповые звонки** идут прежней дорогой — `call.incoming` / `call.state` журналом
устройства. В ПЛАН-ВХОДЯЩЕГО-ЗВОНКА они не входят.

## Связанное

- [ADR-0026](0026-call-journal.md) — **журнал звонков**: источник правды, `ended_by`, `lost`;
- [ADR-0006](0006-livekit-media-policy.md) — медиа-политика LiveKit;
- [ADR-0021](0021-diagnostics-and-problem-reports.md) — отчёты о проблеме, второй источник правды;
- [ПЛАН-ДОРАБОТКИ-ЗВОНКОВ.md §4б](../../doc_mig/ПЛАН-ДОРАБОТКИ-ЗВОНКОВ.md) — полный перечень событий построчно;
- [ПЛАН-ОТЛАДКИ.md §6](../../doc_mig/ПЛАН-ОТЛАДКИ.md) — журнал сравнений провода и отчёта.
